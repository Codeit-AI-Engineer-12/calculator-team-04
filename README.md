# 계산기

## 지원 기능

| 기능 | 함수명 | 설명 | 비고 |
|------|--------|------|------|
| 덧셈 | add | a, b 두 수를 받아 합을 반환함 | 숫자가 아니면 "메롱" 반환 |
| 뺄셈 | subtract | a, b 두 수를 받아 차를 반환함 | 숫자가 아니면 "숫자로 처주세요" 반환 |
| 곱셈 | multiply | a, b 두 수를 받아 곱한 값을 반환함 | 숫자가 아니면 ValueError 발생 |
| 나눗셈 | divide | a, b 두 수를 받아 a를 b로 나눈 값을 반환 | b가 0이면 ValueError 발생 |
| 정수나눗셈 | integer division | a, b 두 수를 받아 a를 b로 나눈 값의 나머지를 제외한 정수 반환 | b가 0이면 ValueError 발생 * |

## 추가하고 싶은 기능

최강의 계산기 모델을 위해 앞으로 구현해볼 기능들을 아래처럼 정리해두면 좋습니다.

| 기능 | 설명 | 상태 |
| --- | --- | --- |
| 나머지 연산 | 두 숫자의 나머지를 구합니다. | 예정 |
| 제곱근 | 숫자의 제곱근 값을 구합니다. | 예정 |
| 로그 | 주어진 수의 로그 값을 계산합니다. | 예정 |
| 팩토리얼 | 자연수의 팩토리얼을 계산합니다. | 예정 |
| 최대공약수 | 두 수의 최대공약수를 구합니다. | 예정 |
| 최소공배수 | 두 수의 최소공배수를 구합니다. | 예정 |
| 절댓값 | 숫자의 절댓값을 반환합니다. | 예정 |
| 삼각함수 | sin, cos, tan 값을 계산합니다. | 예정 |
| 평균 | 여러 숫자의 평균을 계산합니다. | 예정 |
| 팝업/입력 검증 | 잘못된 입력을 더 깔끔하게 처리합니다. | 예정 |

