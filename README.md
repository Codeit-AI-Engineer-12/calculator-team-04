# 계산기

## 지원 기능

| 기능 | 함수명 | 설명 | 비고 |
|------|--------|------|------|
| 덧셈(자연수) | `add` | a, b 두 수를 받아 합을 반환함 | 자연수만 가능 |
| 뺄셈(자연수) | `subtract` | a, b 두 수를 받아 차를 반환함 | 자연수만 가능 |
| 곱셈(자연수) | `multiply` | a, b 두 수를 받아 곱한 값을 반환함 | 자연수만 가능 |
| 나눗셈(자연수) | `divide` | a, b 두 수를 받아 a를 b로 나눈 값을 반환 | b > 0 |